# Azure SDK dependencies
azure-identity>=1.15.0
azure-core>=1.29.0

# Microsoft Graph and service API HTTP client
httpx>=0.24.0

# Excel export
openpyxl>=3.1.0

# Local PDF text extraction and page previews (image-only PDFs use Windows OCR)
PyMuPDF>=1.28.2,<2
